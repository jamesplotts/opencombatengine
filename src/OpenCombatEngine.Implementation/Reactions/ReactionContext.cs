using OpenCombatEngine.Core.Interfaces.Reactions;
using OpenCombatEngine.Core.Interfaces.Spatial;

namespace OpenCombatEngine.Implementation.Reactions
{
    // Defined in StandardReactionManager.cs mostly, but good to have separate file if reused.
    // I'll keep it simple and just rely on the implementation in StandardReactionManager or create a separate one.
    // Let's create a separate file for cleanliness if needed, but StandardReactionManager had it nested/internal-like.
    // Actually, I defined 'ReactionContext' class inside the StandardReactionManager namespace block in the previous tool call.
    // Let's formalize it here.
}
